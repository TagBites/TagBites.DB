<Project>
  <PropertyGroup>
    <AssemblyVersion>1.0.0</AssemblyVersion>
    <VersionPrefix>1.0.1</VersionPrefix>
    <VersionSuffix>preview2</VersionSuffix>
    <InformationalVersion>1.0.1-preview2 2020-03-16</InformationalVersion>
  </PropertyGroup>
    
  <PropertyGroup Condition=" '$(MSBuildProjectName)' == 'TagBites.DB.Postgres' ">
    <VersionSuffix>preview8</VersionSuffix>
    <InformationalVersion>1.0.0-preview8 2020-01-09</InformationalVersion>
  </PropertyGroup>
  <PropertyGroup Condition=" '$(MSBuildProjectName)' == 'TagBites.DB.Npgsql' ">
    <VersionSuffix>preview8</VersionSuffix>
    <InformationalVersion>1.0.0-preview8 2020-01-09</InformationalVersion>
  </PropertyGroup>
  <PropertyGroup Condition=" '$(MSBuildProjectName)' == 'TagBites.DB.Sqlite' ">
    <VersionSuffix>preview1</VersionSuffix>
    <InformationalVersion>1.0.0-preview1 2020-02-03</InformationalVersion>
  </PropertyGroup>

</Project>
